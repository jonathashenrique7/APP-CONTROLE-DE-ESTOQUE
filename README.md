# 📦 Sistema de Gestão de Estoque (v1.0)

Sistema moderno desenvolvido com **FastAPI**, **SQLAlchemy** e **Jinja2**.

### ✨ Funcionalidades atuais:
- CRUD completo de Produtos e Categorias.
- Atualização dinâmica de estoque.
- Dashboard com gráficos (Chart.js).
- Interface Dark Mode com CSS avançado.

### 🚀 Como rodar:
1. Instale as dependências: `pip install fastapi uvicorn sqlalchemy jinja2 python-multipart`
2. Inicie o servidor: `uvicorn main:app --reload`